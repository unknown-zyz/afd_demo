"""gRPC proto + generated stubs for Coordinator control plane.

Stubs (`coordinator_pb2.py`, `coordinator_pb2_grpc.py`) are generated from
`coordinator.proto` via `scripts/gen_proto.sh`. Run that script inside the
NPU container (which has grpcio-tools installed) and check the generated
files in.
"""
